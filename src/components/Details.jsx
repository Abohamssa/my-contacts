import React from "react";

const Details = (props) => {
    return (
        <div>
            <p className="info">{props.detailsInfo}</p>
        </div>
    );
};

export default Details;
